var scores = [45, 81, 73];      // Array of scoresvar arrayLength = scores.length; // Items in arrayvar roundNumber = 0;            // Current roundvar msg = '';                   // Messagevar i;                          // Counter// Loop through the items in the arrayfor (i = 0; i < arrayLength; i++) {  // Arrays are zero based (so 0 is round 1)  // Add 1 to the current round  roundNumber = (i + 1);  // Write the current round to message  msg += 'Round ' + roundNumber + ': ';   // Get the score from the scores array  msg += scores[i] + '<br />';}document.getElementById('answer').innerHTML = msg;// My 2nd Attempt practicing the above script// var scores2 = [51, 35, 99, 85, 48];// var arrayLength2 = scores2.length;// var roundNum = 0;// var message = '';// for (var i = 0; i < arrayLength2; i++) {//   roundNum = (i + 1);//   message += 'Round ' + roundNum + ': ';//   message += scores2[i] + '<br />';// }// document.getElementById('answer').innerHTML = message;