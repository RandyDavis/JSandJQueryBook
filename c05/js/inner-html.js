// Store the first list item in a variable// var firstItem = document.getElementById('one');// // Get the content of the first list item// var itemContent = firstItem.innerHTML;// // Update the content of the first list item so it is a link// firstItem.innerHTML = '<a href=\"http://example.com\">' + itemContent + '</a>';// 2nd timevar firstItem = document.getElementById('one');var itemContent = firstItem.innerHTML;firstItem.innerHTML = '<a href=\"#\">' + itemContent + '</a>';// 3rd timevar secondItem = document.getElementById('two');var nextItemContent = secondItem.innerHTML;secondItem.innerHTML = '<a href=\"#\">' + nextItemContent + '</a>';